# CloudNativePG Operator values
# Ref: https://cloudnative-pg.github.io/charts/

# The operator is installed globally by default
# We enable monitoring for Prometheus/VictoriaMetrics
monitoring:
  podMonitorEnabled: false

# Enable CRD management via Helm
crds:
  create: true
