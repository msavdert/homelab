# Bootstrap Issuer: exists only to self-sign the CA Certificate below. Not
# used directly by any workload Ingress. See ADR-0010.
apiVersion: cert-manager.io/v1
kind: Issuer
metadata:
  name: selfsigned-bootstrap
  namespace: cert-manager
spec:
  selfSigned: {}
