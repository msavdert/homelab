apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: signoz
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "0"
spec:
  project: default
  sources:
    # 1. Source: SigNoz Official Helm Chart
    - repoURL: https://charts.signoz.io
      targetRevision: 0.121.0
      chart: signoz
      helm:
        valueFiles:
          - $homelab-ref/apps/base/signoz/values.yaml
    
    # 2. Source: Reference for values.yaml (Root access)
    - repoURL: https://github.com/msavdert/homelab.git
      targetRevision: main
      ref: homelab-ref

    # 3. Source: Local manifests (onepassword-item.yaml etc)
    - repoURL: https://github.com/msavdert/homelab.git
      targetRevision: main
      path: apps/base/signoz
      directory:
        exclude: "values.yaml"
  
  destination:
    server: https://kubernetes.default.svc
    namespace: signoz
  
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
  
  ignoreDifferences:
    - group: apps
      kind: Deployment
      jsonPointers:
        - /spec/template/spec/containers/0/env/5/resourceFieldRef/divisor
        - /spec/template/spec/containers/0/env/6/resourceFieldRef/divisor
        - /spec/template/spec/containers/0/env/7/resourceFieldRef/divisor
        - /spec/template/spec/containers/0/env/8/resourceFieldRef/divisor
        - /spec/template/spec/containers/1/env/5/resourceFieldRef/divisor
        - /spec/template/spec/containers/1/env/6/resourceFieldRef/divisor
        - /spec/template/spec/containers/1/env/7/resourceFieldRef/divisor
        - /spec/template/spec/containers/1/env/8/resourceFieldRef/divisor
    - group: apps
      kind: StatefulSet
      jqPathExpressions:
        - .spec.template.spec.containers[].env[]?.valueFrom.resourceFieldRef.divisor
        - .spec.volumeClaimTemplates[].apiVersion
        - .spec.volumeClaimTemplates[].kind
